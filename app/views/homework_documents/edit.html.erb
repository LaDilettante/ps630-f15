<% provide(:title, "Edit homework document") %>

<% if @doc.submitter?(current_user.id) %>
  <%= render "submitter_edit" %>
<% elsif @doc.grader?(current_user.id) %>
  <%= render "grader_edit" %>
<% elsif current_user.teacher? %>
  <%= render "teacher_edit" %>
<% end %>